'use strict';
var gulp = require('gulp');

var DIR = {
    SRC: 'src',
    DIST: 'dist'
};

var SRC = {
    JS: DIR.SRC + '/js/*.js',
}

var DIST = {
    JS: DIR.DIST + '/js',
}

gulp.task('default', function(){

});